@model CrudApp.Models.Skills

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Skills</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="skill_id" />
            <div class="form-group">
                <label asp-for="skill_name" class="control-label"></label>
                <input asp-for="skill_name" class="form-control" />
                <span asp-validation-for="skill_name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="information_id" class="control-label"></label>
                <input asp-for="information_id" class="form-control" />
                <span asp-validation-for="information_id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
