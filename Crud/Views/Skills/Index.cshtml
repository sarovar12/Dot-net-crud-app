@model IEnumerable<CrudApp.Models.Skills>

@{
    ViewData["Title"] = "Skills";
}

<h1>Skills</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.skill_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.information_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.skill_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.information_id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.skill_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.skill_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.skill_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
